# The CNN Challenge: 16-Class Scene Recognition from 2,400 Images

**Smriti Bhemireddy** · ITCS 6169/8169 Computer Vision, Assignment 1
**Repository:** https://github.com/smritib4/cnn-challenge-scene-classification

> DRAFT. Cells marked `TBD` are filled from `experiments/results/results.csv` once the
> corresponding run finishes. Two-page limit excludes references.

---

## 1. Final Result

| | |
|---|---|
| **Test accuracy** | **TBD %** (400 images, 25 per class) |
| Validation accuracy of the selected checkpoint | TBD % |
| Architecture | TBD (ImageNet-1k pretrained, fully fine-tuned) |
| Input | 224 × 224 RGB |
| Checkpoint | `runs/best_seed0/best.pt` — TBD link |

**Validation strategy.** The 2,400 training images were split 80/20 into 1,920
training and 480 validation images using a *stratified* split with a fixed seed, so
every class contributes exactly 30 validation images. All architecture and
hyperparameter decisions were made on validation accuracy alone. The 400-image test
set was scored once, on the single checkpoint already chosen, and never used to
select between experiments.

Stratification was not cosmetic. Reproducing the starter's unstratified
`random_split` gave per-class training counts ranging from 115 to 124, whereas the
stratified split gives exactly 120 for every class. On 480 validation images a 1%
difference is about five images, so an unbalanced split injects noise of the same
magnitude as the effects being measured — and because the imbalance is fixed by the
seed, it biases every experiment in the same direction instead of averaging out.

The dataset also ships a third directory, `test2`: 400 unlabelled images named
`image_0.jpg`…`image_399.jpg`. Since it has no labels it cannot be scored;
`predict.py` produces a submission CSV for it, and `scripts/check_split_overlap.py`
hashes both directories to establish whether it duplicates the labelled test split
rather than assuming either way. Result: TBD.

## 2. Secret Recipe

TBD — final settings, once the ladder confirms them. Planned shape, with the reason
each element is present:

- **ImageNet-pretrained CNN backbone, fully fine-tuned.** With 1,920 training
  images, pretrained initialisation is the dominant factor; everything else refines
  it.
- **224 × 224 RGB.** The starter's grayscale 64 × 64 discards colour and spatial
  detail that scene categories depend on.
- **Strong augmentation** (RandomResizedCrop, horizontal flip, RandAugment, colour
  jitter, RandomErasing), because the binding constraint for a large pretrained CNN
  on 1,920 images is overfitting, not capacity.
- **Discriminative learning rates**, backbone 1e-4 and head 1e-3. The head starts
  from noise and must move fast; the pretrained backbone must be protected.
- **Cosine schedule with 3 warmup epochs**, stepped per batch. Warmup prevents the
  untrained head's large early gradients from damaging the backbone.
- **AdamW, weight decay 0.05**, excluded from biases and normalisation parameters.
- **Label smoothing 0.1, weight EMA, horizontal-flip TTA** as variance reducers,
  each verified on validation rather than assumed.

Which of these actually mattered: TBD.

## 3. Experimental Journey

Each row changes approximately one factor from the row above, so differences are
attributable. Generated by `scripts/make_report_table.py` from recorded runs.

| # | Experiment | What changed | Val acc | Observation |
|---|---|---|---:|---|
| 00 | Starter TNet, grayscale 64px | reference | TBD | TBD |
| 01 | + colour, 128px, stratified split | preprocessing only | 58.75 | Large gain with an unchanged architecture: the starter's preprocessing, not its capacity, was the first bottleneck |
| 03 | ResNet-18 linear probe | ImageNet weights frozen, 8,208 trainable params | TBD | TBD |
| 04 | ResNet-18 fine-tuned | unfreeze backbone | TBD | TBD |
| 05 | ResNet-50 fine-tuned | capacity only | TBD | TBD |
| 06 | + strong augmentation | augmentation preset only | TBD | TBD |
| 08 | ConvNeXt-Tiny | architecture family | TBD | TBD |
| — | **Final** | + EMA + flip TTA | TBD | TBD |

Seed-variance check on the final recipe: TBD.

## 4. Failure Analysis

TBD — selected from whichever of these actually failed. Candidates, and why each was
expected to work:

- **Capacity scaling (05 vs 04).** ResNet-50 has richer pretrained features than
  ResNet-18, so it "should" win; with 1,920 images it may simply overfit, in which
  case the lesson is that at this data scale capacity is not the binding constraint.
- **Mixup/CutMix (07).** Label-space regularisation usually helps most when data are
  scarce, but it typically needs long schedules to pay off and may underperform at a
  40-epoch budget.

An engineering failure worth recording separately: `TNet` was first implemented with
`nn.LazyLinear` so it would adapt to any input resolution. Lazy parameters do not
exist until the first forward pass, so `train.py` crashed when counting parameters
beforehand — and because the pipeline smoke test used `resnet18`, the `tnet` path was
never exercised and the failure only appeared on the GPU. The fix computes the
flattened width from `img_size` (57,776 parameters at 64px, matching the starter
exactly), and `scripts/check_models.py` now builds all 16 model/resolution/freeze
combinations in the order `train.py` uses them — counting parameters *before* the
forward pass, which is the only order that reproduces the bug.

## 5. AI + Human

**Where AI helped.** Cursor implemented the config system, argparse plumbing, the
per-family classifier-head replacement across torchvision architectures, and the
plotting utilities. It also wrote a throwaway script that zlib-decompressed the
assignment PDF's object streams to recover the dataset link, which plain text
extraction had missed.

**Where my judgement was required.** Asked to fetch the dataset, the assistant
started a `gdown --folder` crawl that downloaded images one HTTP request at a time.
It was working, and it would have taken about two hours and risked rate-limiting. I
killed it and downloaded the folder as a single archive, keeping reproducibility
through `scripts/prepare_data.py`, which unpacks whatever archive you have *and
verifies it* — per-class counts, matching class names across splits, and a warning if
the training total is not 2,400. Verification is worth more than a scripted download
that is slow and fragile. The general lesson: an agent optimises for "the code runs",
which is not the same as "this is the right approach."

I also rejected the `random_split` pattern that the starter uses and that generated
code reproduces by default, replacing it with a stratified split for the measurement
reasons in section 1.

Full detail in `AI_USAGE.md`.

---

### References

- He et al., *Deep Residual Learning for Image Recognition*, CVPR 2016.
- Liu et al., *A ConvNet for the 2020s*, CVPR 2022.
- Cubuk et al., *RandAugment: Practical Automated Data Augmentation*, NeurIPS 2020.
- Zhang et al., *mixup: Beyond Empirical Risk Minimization*, ICLR 2018.
- Yun et al., *CutMix*, ICCV 2019.
- Loshchilov & Hutter, *Decoupled Weight Decay Regularization*, ICLR 2019.
